# Copie para .env e ajuste conforme necessario.

# Porta em que a API escuta.
PORT=3000

# String de conexao com o Postgres, usada quando a API roda fora do compose.
# No compose (sessao 7) o proprio compose.yaml define DATABASE_URL com host "db",
# entao este valor com localhost nao e usado la.
DATABASE_URL=postgres://recados:recados@localhost:5432/recados

# Credenciais usadas pela imagem oficial do Postgres.
POSTGRES_USER=recados
POSTGRES_PASSWORD=recados
POSTGRES_DB=recados
